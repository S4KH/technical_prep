/* ceil example */
#include <stdio.h>      /* printf */
#include <math.h>       /* ceil */

int main ()
{
  printf ( "ceil of 2.3 is %.1f\n", floor(sqrt(54)) );
  printf ( "ceil of 3.8 is %.1f\n", ceil(sqrt(54)) );
  return 0;
}